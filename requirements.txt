# AI Viral Clipper - Dependencies

# Core
PySide6>=6.5.0
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0
numpy>=1.24.0
torch>=2.0.0
torchvision>=0.15.0

# UI
PySide6>=6.5.0

# Audio Processing
librosa>=0.10.0
pydub>=0.25.1
webrtcvad-wheels>=2.0.11
soundfile>=0.12.1

# Video Processing
moviepy>=1.0.3
scenedetect>=0.6.2

# AI/ML
faster-whisper>=1.0.0
transformers>=4.36.0
sentence-transformers>=2.2.0

# NLP
spacy>=3.7.0
textblob>=0.17.1
vaderSentiment>=3.3.2

# Face Detection & Emotion
insightface>=0.7.3
mediapipe>=0.10.0
fer>=22.0.0

# Configuration
pyyaml>=6.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Utilities
structlog>=24.1.0
tqdm>=4.66.0
pillow>=10.0.0

# Image Processing
scikit-image>=0.22.0

# Note: FFmpeg must be installed separately (required for video processing)
# Download from: https://ffmpeg.org/download.html

# Optional - for improved performance:
# onnxruntime-gpu>=1.16.0
# cuda-python>=11.8

# Optional LLM re-ranking (Phase 2). The tool runs fully without these using the
# free built-in heuristic. Install only the provider(s) you intend to use.
python-dotenv>=1.0.0          # loads API keys from a gitignored .env
google-genai>=0.3.0           # Google Gemini
# openai>=1.30.0              # OpenAI / ChatGPT
# anthropic>=0.40.0           # Anthropic Claude (Haiku)